const SectionTitle = ({heading}) => {
    return (
        <div className="mx-auto text-center bg-black text-white  my-2">
            <h3 className=" uppercase py-2">{heading}</h3>
        </div>
    );
};

export default SectionTitle;